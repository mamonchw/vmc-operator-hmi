version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: vmc_user
      POSTGRES_PASSWORD: vmc_password
      POSTGRES_DB: vmc_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
